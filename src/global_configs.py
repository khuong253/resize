from pathlib import Path

ROOT = f"{str(Path(__file__).parent)}/download"
KMEANS_WEIGHT_ROOT = "clustering_weights"
DATASET_DIR = f"{ROOT}/datasets"
FID_WEIGHT_DIR = f"fid_weights"
JOB_DIR = f"{ROOT}/pretrained_weights"
